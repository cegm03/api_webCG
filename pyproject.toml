[project]
name = "main"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi[standard]==0.139.0",
    "pydantic==2.13.4",
    "python-dotenv==1.2.2",
    "supabase==2.31.0"
]

[tool.fastapi]
entrypoint = "main:app"